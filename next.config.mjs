/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',  // Добавлено для поддержки экспорта
  };
  
  export default nextConfig;  // Используйте export default, а не module.exports
  